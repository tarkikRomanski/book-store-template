$(document).ready(function() {
	/*
	$(window).resize(function(){
		if($(window).width() <= 768) {
			$('#categories_collapse').attr({
				'data-target': '#categories_list',
				'data-toggle': 'collapse',
				'aria-expanded': 'false',
				'aria-controls': 'categories_list'
			});
			$('#categories_list').addClass('collapse');
		} else {
			$('#categories_collapse').attr({
				'data-target': '',
				'data-toggle': '',
				'aria-expanded': '',
				'aria-controls': ''
			});
			$('#categories_list').removeClass('collapse');
		}
	});
	*/
});